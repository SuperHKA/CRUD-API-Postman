$kind: http-request
name: '13 POST con JSON invalido'
method: POST
url: '{{base_url}}/api/products'
order: 13000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{jwt_token}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: '{ "codigo": "BAD", '
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Estado 400', function () { pm.response.to.have.status(400); });
