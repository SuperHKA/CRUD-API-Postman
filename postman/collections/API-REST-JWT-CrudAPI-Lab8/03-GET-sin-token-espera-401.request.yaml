$kind: http-request
name: '03 GET sin token espera 401'
method: GET
url: '{{base_url}}/api/products'
order: 3000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Estado 401', function () { pm.response.to.have.status(401); });
